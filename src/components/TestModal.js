import React from 'react';
import Modal from 'react-modal';

const TestModal = () => (
    <Modal isOpen={true}>
        <h2>Modal title</h2>
        <p>Modal body</p>
    </Modal>
);

export default TestModal;